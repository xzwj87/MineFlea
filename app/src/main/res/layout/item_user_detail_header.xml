<?xml version="1.0" encoding="utf-8"?>
<FrameLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:orientation="vertical"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    app:layout_collapseMode="parallax"
    app:layout_collapseParallaxMultiplier="0.7">

    <ImageView android:id="@+id/head_icon"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:scaleType="centerCrop"
        android:contentDescription="@string/user_head_icon" />

    <LinearLayout
        android:layout_marginStart="@dimen/user_detail_title_margin"
        android:layout_marginTop="@dimen/user_detail_title_margin"
        android:orientation="vertical"
        android:gravity="center|start"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content">

        <TextView android:id="@+id/tv_user_nick_name"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:textStyle="bold"
            android:textColor="@color/colorWhite"
            android:textSize="@dimen/user_detail_title_size" />

        <TextView android:id="@+id/tv_user_email"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:textColor="@color/colorWhite"
            android:textSize="@dimen/user_detail_text_size"/>
    </LinearLayout>

    <TextView android:id="@+id/tv_user_action"
        android:background="@drawable/item_rect_background"
        android:drawableStart="@drawable/ic_edit_white_24dp"
        android:drawablePadding="8dp"
        android:text="@string/edit"
        android:textColor="@color/colorWhite"
        android:layout_marginBottom="@dimen/user_detail_text_margin"
        android:layout_marginEnd="@dimen/user_center_title_margin"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_gravity="bottom|end"/>

</FrameLayout>